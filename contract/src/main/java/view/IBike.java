package view;

import controller.IElement;

public interface IBike extends IElement {

	/**
	 * Get the value in the game in general
	 * 
	 * @return Get the value of the monster
	 */
	int getValue();
}
